name: Checking dangling commits by Trufflehog

permissions:
  contents: read
on:
  workflow_dispatch:
    inputs:
      workingOrgs:
        description: 'Comma-separated orgs to scan'
        default: 'axonivy-market'
      workingRepos:
        description: 'Comma-separated list of repo names (optional)'
        default: ''

jobs:
  prepare:
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ github.token }}
    outputs:
      repo-matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - name: Generate matrix dynamically
        id: set-matrix
        env:
          ORG: ${{ github.event.inputs.workingOrg }}
        run: |
          echo "🔍 Fetching repos from $ORG"
          REPOS=$(gh repo list "$ORG" --limit 1000 --json name -q '.[].name')
          MATRIX_JSON=$(jq -n --argjson repos "$REPOS" '{ include: $repos }')
          echo "matrix=$MATRIX_JSON" >> $GITHUB_OUTPUT
        shell: bash

  truffle-scan:
    needs: prepare
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        repo: ${{ fromJson(needs.prepare.outputs.repo-matrix).include }}
    env:
      ORG: ${{ github.event.inputs.workingOrg }}
    steps:
      - uses: actions/checkout@v4
      - name: Install TruffleHog
        run: pip install trufflehog jq
      - name: Run TruffleHog
        run: |
          REPO="${{ matrix.repo }}"
          echo "🚨 Scanning $ORG/$REPO ..."
          mkdir -p trufflehog-reports
          trufflehog --json --regex --rules ./trufflehog-custom-rules.json "https://github.com/$ORG/$REPO.git" \
            | jq '.' > "trufflehog-reports/${ORG}__${REPO}.json" || true
      - uses: actions/upload-artifact@v4
        with:
          name: trufflehog-${{ matrix.repo }}
          path: trufflehog-reports/
